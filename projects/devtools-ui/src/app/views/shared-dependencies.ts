import { Component } from '@angular/core';

@Component({
  selector: 'nf-shared-dependencies',
  template: `
    <section class="view">
      <h1>Shared Dependencies</h1>
      <p class="view-placeholder">
        Shared-dependency resolution outcomes will appear here once a snapshot
        is loaded.
      </p>
    </section>
  `,
})
export class SharedDependencies {}
